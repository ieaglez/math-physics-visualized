# 数理可视课堂 · Math & Physics Visualized

面向高中生的**交互式数学·物理可视化学习网站**，38 个专题覆盖高一到高三主干知识。每个专题包含：

- 🎛 **交互实验**：拖动滑块改变参数，图形/动画实时变化
- 🧮 **核心公式**：页面顶部横幅 + 参数面板迷你卡，随手可查
- 📖 **中文原理讲解** + 英文术语 (English terms) + 实验建议与思考题
- 🗺️ **学习路径**：8 站循序渐进（与人教版进度大体同步），标注前置知识与"下一站"
- ✅ **学习进度**：每个专题可打卡"已掌握"，进度存于浏览器 localStorage（无需注册）
- 🌐 **中英双语**：侧栏一键切换 中文 ⇄ English，全站内容（讲解、公式、读数）双语；语言偏好同样存于 localStorage。代码中用全局 `L(中文, English)` 辅助函数内联维护两种文案，新增内容请保持该模式

纯静态网页（HTML + CSS + 原生 JavaScript Canvas），**零依赖、无需构建**。

## 本地使用

直接双击打开 `index.html` 即可；或启动任意静态服务器：

```bash
npx http-server -p 8080
# 打开 http://localhost:8080
```

## 部署上线（免费）

整个目录就是网站本身，可直接部署到：

- **GitHub Pages**：仓库 Settings → Pages → 选择分支根目录
- **Netlify / Vercel / Cloudflare Pages**：拖拽文件夹即可

## 课程目录

| 分类 | 专题（38 个） |
|---|---|
| 数学 · 函数与几何 (17) | 集合与韦恩图 · 二次函数 · 基本不等式 · 指数与对数函数 · 三角函数与单位圆 · 函数图象变换 · 解三角形 · 平面向量 · 数列 · 复数与复平面 · 导数与切线 · 直线与圆 · 椭圆 · 双曲线与抛物线 · 立体几何（柱锥球）· 二项分布 · 正态分布 |
| 物理 · 力学 (10) | 匀变速直线运动 · 力的合成与分解 · 斜面受力分析 · 抛体运动 · 匀速圆周运动 · 万有引力与卫星 · 机械能守恒（单摆）· 动量守恒与碰撞 · 简谐运动（弹簧振子）· 机械波 |
| 物理 · 电磁与光学 (8) | 库仑定律与电场 · 欧姆定律与电路 · 洛伦兹力 · 电磁感应 · 交流电与变压器 · 光的折射与全反射 · 凸透镜成像 · 光的双缝干涉 |
| 物理 · 热学与近代物理 (3) | 理想气体定律 · 光电效应 · 氢原子能级 |

学习路径定义在 `js/app.js` 的 `LEARNING_PATH`（8 站）与 `PREREQS`（前置知识）中；新增专题后记得把它加进某一站。

## 如何新增专题

1. 在 `js/topics/` 下新建一个文件，调用 `registerTopic({ id, cat, icon, title, en, desc, render })`；
   - `cat` 取值：`math` / `mech` / `optics`（在 `js/app.js` 的 `CATEGORIES` 中可增加新分类）
   - `render(root)` 中用 `topicPage()` 搭骨架、`createCanvas()` + `Plot` 画图、`addSlider()` 加控件，返回清理函数
2. 在 `index.html` 中加一行 `<script src="js/topics/xxx.js"></script>`（在 `app.js` 之前）

公共工具见 `js/utils.js`：坐标系绘图 `Plot`、箭头 `pxArrow`、弹簧 `drawSpring`、动画器 `makeAnimator`、滑块/分段选择/读数面板等。
